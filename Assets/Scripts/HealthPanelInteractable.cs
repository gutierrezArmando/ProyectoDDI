using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HealthPanelInteractable : MonoBehaviour {


    private float alphaLevel = 0;
    public GameObject skeleton;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
/*        if (Input.GetKey(KeyCode.A)|| skeleton.GetComponent<SkeletonBehavior>().isDamaging)
            alphaLevel += 0.005f;
        if (Input.GetKey(KeyCode.S))
            alphaLevel-=0.005f;
        this.GetComponent<MeshRenderer>().material.color = new Color(255f, 0, 0, alphaLevel);*/
	}

}
